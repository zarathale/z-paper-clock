---
date: 2026-05-05
start_time: "23:55"
end_time: "23:55"
mode: cowork
participant: Zarathale (Alan)
---

## Goal

Review Zarathale's split of piece 93 into 093a + 093b authoring templates, propagate the split through `pieces.csv` and the related authoring-side configs, and document the brace-assembly model so the (a+b) pair is unambiguously the template that gets instantiated downstream.

## What was done

**Reviewed `093.svg`, `093a.svg`, `093b.svg` against `LAYER-CONVENTIONS.md` and `CLAUDE.md`.**

The split is the right call: book §II.E and `embedded-labels.md:33` describe piece 93 as multiple folded brace shapes, and `source/pieces/093.png` shows two visibly distinct shapes on the plate. Treating the smaller (rectangle + triangle, 2 panes) as 093a and the taller (hex with side tab, 3 panes) as 093b parallels the existing `092a` / `112a` letter-variant pattern.

Findings (initial review pass — first message in this session):

- **Combined `093.svg` had an Affinity collision baked in.** Authoring used the same id `attach-x093a` on a `<rect>` in `attach-points` and a `<path>` in `marks`; on export Affinity disambiguated the second to `id="attach-x093a1" serif:id="attach-x093a"`. The split halves don't have this problem.
- **Cross-half attach naming is clean and worth formalizing later.** 093a contains `attach-x093b` (i.e. "the place 093b lands on me") and 093b contains `attach-x093a` (after Zarathale's same-session revision). Each side names the other by the partner's piece id — the same spirit as the `landing-c70` ⟷ `tab-c` cross-piece pairing, but for glue-only connections that don't have a printed tab letter. The `attach-points` layer name and the `attach-x<piece-id>` id form aren't currently in `CLAUDE.md` / `LAYER-CONVENTIONS.md`. Worth raising as a convention proposal in a future session.
- **Cutaway naming slip:** `cutaway1` / `cutaway2` should be `cutaway-1` / `cutaway-2` per the 2026-05-02 cut-layer convention. Currently works because each split file has only one (parser's single-piece path catches them), but doesn't match the spec.
- **No fold paths** in either half despite both shapes having visible printed fold lines. The `panels` + `pane1`–`pane5` layer encodes face regions instead of the folds themselves; `preview.html` won't be able to fold the braces until those lines are authored as paths in `<g id="folds-valley">` or `<g id="folds-mountain">`. Open question whether `panels` is QA scaffolding to keep or supersede.
- **Tangential transcription anomaly:** `embedded-labels.md:33` says "Six small folded brace shapes shown in two groups, each labeled '93'" but `source/pieces/093.png` shows only two shapes. Either the count in the labels is off or the source crop is missing instances. Logged for a future transcription-QA pass; not in scope for this session.

**Recorded the assembly model in `pieces.csv` notes.** Per Zarathale's mid-session clarification: the (093a + 093b) pair is the brace template, instantiated **six times** in piece 94, with each instance possibly orienting a relative to b slightly differently. The book wording is "Glue the six pieces designated 93 together to form *three separate* braces" — so the build (6 instances) deviates from the book (3 instances). Flagged in Zarathale's awareness mid-session; build count of 6 is the intent of record. Per-instance a/b orientation is assembly-side data (M4 territory), not template data.

**Replaced the `093` row in `pieces.csv` with `093a` + `093b` rows.** Status flipped to `traced` for both (the SVGs ship at `work/pieces/093/093a.svg` and `work/pieces/093/093b.svg`). Notes capture: the partner pairing, the build's 6-instance count, and a one-line callout that the build deviates from the book's 3-brace count.

**Propagated the split through derivative configs and the generator** so `pieces.csv` regeneration won't undo the change:

- `work/scripts/build_master_list.py` — replaced the `093` seed-list tuple with paired `093a` + `093b` tuples, notes mirroring `pieces.csv`.
- `work/piece_characters_v2.yaml` — replaced the `"093"` entry with `"093a"` + `"093b"`, both `character: flat`, `subtype: bob-brace`, with partner notes.
- `work/expected_layers.yaml` — replaced `"093"` (which removed `folds-valley`, `folds-mountain`, `axles`) with `"093a"` + `"093b"` that remove only `axles`. The braces have internal folds, so the audit should expect `folds-valley` / `folds-mountain` once those layers are authored.
- `claude-work/STATUS.md` line 72 — updated the bob-pieces batch list from `093` → `093a/093b` with a one-line callout of the split.

**Did not modify:**

- `work/state.json` — auto-generated by the audit script; will pick up the new rows on next run.
- `source/transcriptions/embedded-labels.md` — transcription source-of-truth; the (possible) six-shape-count anomaly is logged here for a future QA pass, not edited.
- Earlier session notes (`2026-05-05-1630_*.md`, `2026-05-05-2330_*.md`) that mention `093` — historical records, left as-is.

**Suggested follow-up tweaks for Zarathale's next pass through 093a/093b in Affinity:**

1. Rename `cutaway1` → `cutaway-1` (in 093b) and `cutaway2` → `cutaway-2` (in 093a) to match the 2026-05-02 cut-layer convention.
2. Decide whether `panels` + `pane1`–`pane5` is staying as QA scaffolding or being replaced with proper fold paths in `folds-valley` / `folds-mountain` so the viewer can fold the braces.
3. Retire the combined `work/pieces/093/093.svg` to `work/pieces/093/_attic/093.svg` (or delete) — it has the Affinity `attach-x093a1` collision baked in and isn't the source of truth anymore.

These are non-blocking; pieces.csv now treats 093a + 093b as the canonical templates regardless.

## Open questions

- Build count of 6 brace instances vs book's 3 — Zarathale stated 6, captured as such in `pieces.csv` notes with a "build deviates" callout. Easy to flip to 3 in a future session if it turns out 6 was a slip.
- `attach-points` layer + `attach-x<piece-id>` id format need formal CLAUDE.md / LAYER-CONVENTIONS.md treatment before more pieces follow this pattern. Worth landing a small convention pass next time glue-only inter-piece connections come up.
- Cross-piece glue-only connection naming — should it move into `marks` (extending the landing convention with a non-tab-letter form) or stay as a separate `attach-points` layer? Open.

## Next-session handoff

When Zarathale next sits down with 093a / 093b in Affinity:

- Apply the three suggested tweaks above (cutaway hyphenation, panels/folds decision, retire combined 093.svg).
- If you want assembly-side work next (per-instance a/b orientation transforms for the 6 brace instances), that's M4 territory and lives in `work/assemblies/` once that folder gets populated — not blocking anything currently active.

If a fresh session picks up the convention thread (formalizing `attach-points` / `attach-x<piece-id>`), that's a CLAUDE.md + LAYER-CONVENTIONS.md edit, no SVG changes.
